language: node_js
node_js:
  - 9
script:
  - npm run build
after_script:
- bash ./build/deploy_to_gh_pages.sh
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $Travic-cl  # Set in travis-ci.org dashboard, marked secure
  keep-history: true
  local-dir: build
  on:
    branch: master

